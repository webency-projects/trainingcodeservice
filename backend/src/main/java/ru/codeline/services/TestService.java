package ru.codeline.services;

import ru.codeline.dto.TestRequest;
import ru.codeline.models.course.Lecture;
import ru.codeline.models.course.Test;

public class TestService {
    /*private void addTest(Lecture lecture, TestRequest testRequest) {
        if (testRequest != null) {
            Test test = new Test();
            test.setInput(testRequest.getInput());
            test.setOutput(testRequest.getOutput());
            test.setLecture(lecture);

            lecture.setTest(test);
        }
    }*/
}
